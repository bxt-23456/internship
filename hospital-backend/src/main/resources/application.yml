# 经常修改的链接配置
app:
  #5500的前端地址，最后要带上'/'
  frontend-base-url: "https://66617a45.r35.cpolar.top/"
  alipay:
    #8080的后端地址，替换'/alipay/notifyUrl'和'/alipay/returnUrl'前面部分
    notify-url: "https://33401ee1.r35.cpolar.top/alipay/notifyUrl"
    return-url: "https://33401ee1.r35.cpolar.top/alipay/returnUrl"
  cors:
    #5500和8080全部增加进去
    allowed-origins:
      - "http://localhost:63342"
      - "http://localhost:8080"
      - "http://127.0.0.1:63342"
      - "http://127.0.0.1:8080"
      - "http://127.0.0.1:5500"
      - "http://localhost:5500"
      - "http://localhost:57596"
      - "https://33401ee1.r35.cpolar.top"
      - "https://66617a45.r35.cpolar.top"

spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    timeout: 6000
  datasource:
    url: jdbc:mysql://localhost:3306/hospital_db?serverTimezone=UTC
    username: root
    password: zhaohao20050916
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 20
      min-idle: 10
      max-active: 100
# JWT配置
jwt:
  secret: hospital_secret_key_2024_very_long_and_secure
  expiration: 86400000  # 24小时（毫秒）- JWT仅作为标识，实际状态由Redis管理

# 用户登录状态配置
user:
  token:
    expiration: 1800  # 登录状态过期时间（秒），每次请求重置

# 阿里云短信配置



mybatis-plus:
  type-aliases-package: com.whlg.hospital.entity,com.whlg.hospital.dto,com.whlg.hospital.vo	#指定别名实体类的包
  mapper-locations: classpath:mapper/*.xml  #指定Mapper映射文件的路径
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 指定日志
    map-underscore-to-camel-case: true    #开启驼峰命名
