<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.whlg.hospital.mapper.ReviewMapper">

    <!-- 查询指定医生的评价列表（关联用户表获取用户名和头像） -->
    <select id="selectReviewVoByDoctorId" parameterType="long" resultType="ReviewVo">
        SELECT r.id, r.user_id userId, u.username, u.avatar userAvatar,
               r.rating, r.content, r.order_type orderType, r.create_time createTime
        FROM t_review r
        LEFT JOIN t_user u ON r.user_id = u.id
        WHERE r.doctor_id = #{doctorId}
        ORDER BY r.create_time DESC
    </select>

</mapper>